# 検証記録 — 2026-09-21

## Mel-Band分離比較

audio-separator0.47.0、Kimberley Jensen版の固定重み・YAML、PyTorch2.8.0+cu128の独立環境で検証。RTX2080で8秒窓・overlap4・autocast、通信禁止で2曲の分離とAST/YAMNet比較が完了した。通常の4ステム・全トラック・series・手修正・元音源・再生PCMの保持をMCP経由の実行ハーネスで照合した。

以下は標準しきい値での候補区間数（ビートボックス / ブレス / ハミング）。正解率・再現率の評価ではない。

| 曲 | 検出器・入力 | 候補数 |
| --- | --- | --- |
| 検証曲A | AST・Demucs | 1 / 4 / 2 |
| 検証曲A | AST・Mel-Band | 2 / 2 / 2 |
| 検証曲A | YAMNet・Demucs | 0 / 3 / 2 |
| 検証曲A | YAMNet・Mel-Band | 1 / 1 / 1 |
| 検証曲B | AST・Demucs | 0 / 6 / 1 |
| 検証曲B | AST・Mel-Band | 0 / 7 / 4 |
| 検証曲B | YAMNet・Demucs | 2 / 3 / 8 |
| 検証曲B | YAMNet・Mel-Band | 0 / 5 / 5 |

検証曲AのMel-Band入力ではASTが冒頭0～1秒を追加検出、YAMNetは57.12秒からの窓を検出した。一方でブレス・ハミング候補が減った例もある。候補の増減を音声保持の改善と断定せず、同位置でボーカル・伴奏を試聴して判断する。時間付き正解ラベルによる定量的精度評価と、人による聴感評価は未実施。

完了run: 検証曲A `20260921T044754-e27462f4`、検証曲B `20260921T045230-7fa88ddf`。検証用コピーへ保存し、利用者の保存曲へは自動適用していない。

2曲目の初回は進捗status.jsonの置換でWindowsのWinError5が発生した。共有違反の上限付き再試行と、失敗時に旧ファイルを保持するテストを追加。分離キャッシュを再利用した再試行で完了した。実ファイルのキャッシュ一致時に推論プロセスを起動しないこと、追加2音声をヘッドレスAPIで切り出せることも確認した。

`npm run check` のUI40・Python51・MCP1・Rust2が成功。保存競合修正後はPython53テストが成功し、合計96テスト。型検査・Vite・rustfmt・Clippy成功。最終 `npm run tauri:build -- --no-bundle` でWindows実行ファイルを更新。

ブラウザーハーネスで6通りのスコア表示、Mel-Band候補から57.12秒への移動、ループ保持、同位置でMel-Band伴奏への切替、音声読み込みエラーなし・自動再生なしを確認。Tauri標準1440×920相当のレイアウトを確認した。ネイティブGUIのクリック操作は未実施。CPU全曲推論、実GPUメモリー不足による401窓への切替は未検証（401窓自体はセットアップ時の試験推論に使用）。

## 声の表現の追加

AST AudioSet分類を追加。固定リビジョン `f826b80d28226b62986cc218e5cec390b1096902`、2秒窓・0.5秒hop、原曲＋分離ボーカル、sigmoidの多ラベル判定。標準閾値0.15、候補多め0.06。生の窓スコアと参照音声を各runの `vocal-events-evidence.json` に保持する。APIへの音源送信は行わず、推論時のネット接続を禁止して実行した。

検証曲Aの冒頭約1秒と歌詞終了後を確認対象にした。標準設定で0.0〜1.0秒、57.5〜60.2秒にビートボックス候補を得た。ほかの箇所には未確認の候補があり、全体の適合率・再現率は未測定。ブレスとハミングの正解時刻も未提供。通常歌唱や楽器の誤検出を排除できたという意味ではない。

2曲の声の表現のみの解析はcomplete。「検証曲A」は10候補・約26秒、「検証曲B」は9候補・約11秒。時間はこの実行の参考値。歌詞の自動結果と手修正が実行前後で同一であること、JSON/CSV/SRT書き出しを確認。SRTへ声の表現を混入させない。結果は `test-results/vocal-events-verification.json`。

画面ハーネスで種類別の表示、末尾候補の選択、スコアと参照音声、分類変更、元の推定情報の保持、Undoを確認。実曲プロジェクトの手修正は変更していない。UI16件・Python17件・Rust1件の合計34件が成功し、Windows実行ファイルを再ビルドした。

24秒の合成インストでも、新しい検出段階を含む全体解析がcomplete（約38秒、分離キャッシュ使用）。声の表現候補は0件、歌詞も空のまま保持された。記録は `test-results/vocal-full-pipeline.json`。

## ファイル選択・終了処理の修正

曲取り込み時は音源と保存先の2つのダイアログを連続で開いていた。音源選択1回に変更し、保存先はOSのドキュメント/Music Sweeper/Projectsへ統一した。既存プロジェクトは移動していない。

終了ハンドラーはTauri SDK内部でdestroyを呼ぶが、mainウィンドウにallow-destroyがなかった。権限を追加し、終了を一度保留して必要な保存を待ち、明示的にdestroyする処理に変更した。終了の再入、保存失敗、権限等の終了失敗を扱い、WebViewではブラウザー用beforeunload警告を重ねない。

追加した回帰テストは6件（音源選択1回、キャンセル、未編集終了と権限、保存待ちと終了連打、保存失敗と再試行、destroy失敗の表示）。合計UI14件・Python11件・Rust1件が成功。ネイティブの×ボタンを操作するGUI E2Eは未実施。

Windows / NVIDIA RTX 2080 8GB / メモリー32GB / Python3.11 / PyTorch2.8.0+cu128で実行。初回モデル取得後、解析プロセスのネット接続を禁止して検証した。実曲の元ファイルには書き込まず、プロジェクト内コピーを使用した。

## 実曲の全工程

| 音源 | 読み込んだ長さ | 日本語モード | 拍 | コード候補 | 構成候補 | 歌詞行 | 時刻未確定行 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 検証曲A.mp3 | 60.20秒 | complete / エラー0 | 104 | 21 | 4 | 7 | 1 |
| 検証曲B.mp3 | 60.76秒 | complete / エラー0 | 100 | 19 | 5 | 5 | 0 |

波形・分離・拍・BPM・コード/キー・All-In-One構成・歌詞/時刻・音高まで実行し、JSON/CSV/SRTを出力した。最新の実行時間はそれぞれ169秒・157秒（分離キャッシュ使用、ビルド等と並行実行）。速度の保証値ではない。分離からの全工程も別runで成功している。

保存先は `test-results/projects.json` に記録。最新runは各プロジェクトの `project.json` が指す。エンジン名、原本SHA256、要求、段階状態、未編集の推定結果、ASR生出力を保持する。

## 日本語と多言語の比較

同じ分離ボーカルに対し、多言語モード（言語自動判定・beam5）も実行。両曲とも日本語と判定された。「検証曲A」は8行・未確定1行、「検証曲B」は5行・未確定0行。日本語固定・beam10とは分割や認識内容に差があった。比較結果は各プロジェクトの `validation-multilingual/` に保存。

正解の歌詞と時刻は提供されていないため、CER/WER・境界誤差・日本語モードの優位性は未評価。歌詞の取り違え・欠落を確認している。「検証曲A」では終了挨拶のような幻覚出力があり、alignment失敗時は時刻未確定のまま保持した。正常終了は認識内容の正しさを意味しない。日本語以外の実歌唱、日英混在の区間再解析は実曲で未検証。

## 合成音・保存・画面

- 24秒・120 BPMの和音＋パルスを作り、WAV/MP3/M4A/MP4/FLACの5形式で取り込み成功。インストモード全工程成功、推定120 BPM、歌詞は空、4ステムと音高曲線を生成。記録は `test-results/synthetic/verification.json`。
- Pythonテスト11件: 既知C/Am和音、無音、手修正BPM、Unicodeパス、原本コピー、保存競合、再解析後の修正保持、未確定歌詞の字幕除外、破損入力の片付け、パス逸脱/不正時刻、CLI応答、原本改変検出、中止状態の復帰など。
- UIテスト8件: プレビュー境界、プロジェクトを開く、編集保存、失敗表示、日本語モード引き渡し、音声切り替え時のシーク維持、編集の純粋処理。
- ブラウザーハーネスを実曲データで操作し、歌詞修正・Undo/Redo・メモリー内保存・ボーカル再生・シーク維持・区間ループ・1440幅の表示を確認。元プロジェクトの歌詞は変更していない。
- RustのIPC契約テスト、fmt、Clippy、TypeScript/Viteビルドを実行。Windows release実行ファイルをビルドし、起動後7秒間正常に存続することを確認。

ネイティブのファイルダイアログから解析・保存までをGUI操作するE2Eは未実施。ネイティブ終了時の自動保存、実行中のプロセスツリー停止、15分の実曲、GPUメモリー不足、別PCでの新規セットアップ/インストールは、実環境での追加確認が必要。

## 再実行

```powershell
npm run check
npm run tauri:build -- --no-bundle
.runtime/venv/Scripts/python.exe scripts/verify-analysis.py
```

最後のコマンドはGPUモデルを使用する統合ハーネスで、合成音だけを扱う。CIの通常checkには含めない。モデル、実曲、生成ステム、ハーネスデータはGit対象外。

## 音量調整と非言語音の表記（2026-09-21）

再生音量0～100%・ミュートを追加。コンポーネントテストで音声要素への反映、分離音声切り替え後の保持、再マウント後の復元、不正設定値のフォールバックを検証。声の「内容」に入力した表記は分類変更後も保持する。非言語音の自動文字起こしは未対応であることをUIに明記。


## ラップ・朗読／語り（2026-09-21）

準備済みASTのRappingとNarration, monologueを追加。Speech一般は歌唱やラップを語りへ広く誤分類する可能性があるため代用しない。分類は重複可能で、歌詞と手修正を変更しない。モデルスコアは正解率ではない。

許可済みの「検証曲A」「検証曲B」で実行し、標準閾値0.15では両分類とも0件。「検証曲A」の最大スコアはラップ0.0216、語り0.0370、「検証曲B」は0.0347、0.0584。10秒窓の比較でも改善せず（それぞれ0.0119/0.0328、0.0152/0.0294）、採用していない。閾値を結果に合わせて下げず、実験的機能・見逃しありと表示。実曲でラップ・語りを正確に検出できたとは扱わない。報告はtest-results/rap-verification.json、比較はcompare-delivery.log。声のみの再解析でも既存歌詞とeditsが不変なことを検証。

手修正トラックへ、未登録の分類の自動候補を明示操作で追加できる。追加IDは新規生成し、旧モデルの行IDとの衝突を避ける。表示・修正保持・重複分類・保存/CSV出力をテスト。

## 音楽プレイヤーUI（2026-09-21）

Playerコンポーネントに再生/一時停止、シーク、10秒戻る/進む、先頭、ループをまとめ、下段に分離音声・音量・時間軸ズームを配置。曲名の重複と装飾、消音ボタンを省いた高さ約90pxのコンパクトな2段構成。旧ミュート設定は音量0に移行し、スライダーを上げれば再生音が戻る。

ブラウザーハーネスで外観、10秒送り、キーボードシークを確認。再生位置の上限・下限、音量の保持、ループのテストを実行。ネイティブ画面の自動操作はしていない。

## 保存した曲の一覧（2026-09-21）

Documents/Music Sweeper/Projects内の既存プロジェクトを発見し、開いた外部プロジェクトの場所もアプリデータ/libraryへ記録。起動後に一覧から解析済みデータを再利用できる。曲切り替え前に適用済み手修正を保存。存在しない/壊れた一覧項目を除外し、同一パスは重複表示しない。

npm run checkとWindowsビルド成功。UIテストで一覧クリック時にimport/analysisを呼ばずsnapshotを開くことを確認。Rustテストでファイルからの登録復元、標準保存先との重複排除、破損項目と移動済みデータの除外を確認。ブラウザーハーネスで一覧表示と保存した解析の表示を確認。

## 保存済み音声の再生エラー修正（2026-09-21）

Rustのcanonicalizeが返すWindows拡張パス（\\?\）へ、UIが /audio.wav を連結していた。Windows拡張パスはスラッシュ区切りを受け付けず、Tauri asset protocolのFile::openが失敗する。実プロジェクトで混在区切りのopenがInvalid argumentになり、バックスラッシュ統一でRIFF/WAVEを読めることを再現。

src/api.tsのassetで、Windows形式のパスだけ区切りをバックスラッシュに統一し、拡張パスと長いパスの接頭辞は維持。Unix形式は保持。拡張ドライブ、拡張UNC、日本語、分離音声の階層、通常パスを回帰テスト。標準保存先5プロジェクト・25音声ファイルのヘッダー読み込みを確認。音源や解析結果は変更していない。ネイティブ画面の再生そのものは自動操作していない。

## ビートボックス補助検出とコードの曖昧さ（2026-09-21）

声の種類の分類とは別に、分離ボーカルのHPSS打楽器成分から連続するオンセットを拾う候補生成を追加。既定で有効だがチェックで解除可能。元のモデル候補と重なる範囲は差し引く。model scoreを捏造せず、method=vocal-percussion、未確認、誤検出の注意とともに保存。手修正トラックへ追加する明示操作を拡張し、分類を変えた旧「その他」を復活させない。

許可済み「検証曲A」では補助候補7区間（1～3.77、6.42～8.59、11.05～12.83、14.95～15.52、21.1～30.0、34.54～38.67、43.35～46.38秒）。「検証曲B」でも13区間が出るため、分類精度の改善を証明したものではなく見逃し対策の候補生成。ラップ/歌唱の子音や楽器漏れの確認が必要。

コード入力を分離ベース＋その他へ変更（分離不可の場合は原曲）。伴奏だけでも候補の揺れは減らなかったため、上位テンプレートの差が小さい場合は未確定とする。信頼判定は未校正のヒューリスティックで、和音の真値検証ではない。「検証曲A」は従来21区間から10区間（うち5区間未確定）。単音に長短三和音を断定しないテストと、既知C/Amのテストが通過。比較結果はtest-results/percussion-review.json。

## 保存曲の解析データ削除（2026-09-21）

保存曲の×ボタンから対象名と削除内容を確認して実行する。元音源・プロジェクト内の原本コピー・audio.wavを保持し、runs/history/exportsと手修正を削除する。Python storageで削除対象をプロジェクト内へ制限し、原本や再生音声を包含する対象は拒否。Rust側は解析ジョブのロックを保持して削除と解析開始の競合を防ぐ。

一覧の削除マーカーはアプリデータ/library/hiddenに保存。標準保存先の再走査やsnapshotポーリングで復活しない。明示的にフォルダー選択して開くと再登録。実ユーザーデータの削除は実行せず、合成プロジェクトで音源バイト列の保持、解析フォルダー消去、音源が対象内にある場合の拒否を確認。UIで確認前/キャンセル時に削除しないことも検証。

## コード候補の表示と単独再推定（2026-09-21）

上位三和音の差が小さいだけでは未確定にせず、コード名とuncertain=trueを返す。タイムラインは?、一覧は候補、詳細は低確度の注意を表示。単音相当の音高支持しかない/適合が弱い区間は未確定に留める。既知C/Amと単音の回帰テストを維持。これは候補を表示する改善であり、コードの正解率向上を証明する変更ではない。

「検証曲A」の未確定は約1.64秒、「検証曲B」は約14.06秒。弱い候補は多く、試聴確認が必要。scope=harmonyによりコードとキーだけ新runに再推定し、他トラック・シリーズ・手修正を保持。参照プロジェクト「検証曲A」で実行し、コード/キー以外のトラックと手修正の一致を確認。

## ChordMini比較導入（2026-09-21）

公式コミットaa6e3a8d7b017f082fd2aaff9329d5c26af49c03。独立Python3.11、torch2.9.1+cpu、librosa0.10.1。両チェックポイントの全重みをstrict=Trueで読み込めた。BTCの平均-2.369753837585449、標準偏差1.9627078771591187、108フレーム、144ビン。ChordNetも自身のチェックポイント統計を使用する。CQT・正規化・窓統合・出力区間化は公式関数を使用し、最低区間長0。BTCの「検証曲A」出力は公式test.py CLIの同条件出力とファイルハッシュが一致。

許可済み2曲の参照プロジェクトで、元音源によるBTC、BTC＋Gaussian kernel3、ChordNetを実行。歌詞・声の表現・拍・手修正の不変をassertで確認。元ファイルと実ユーザーの保存曲は変更していない。方式ごとに従来方式も伴奏＋既存Viterbiで算出したため、下表はモデル単体の公平な精度評価ではなく、実際のパイプラインの出力比較である。

| 曲 | 方式 | 全区間数 | 0.5秒未満の区間数 |
| --- | --- | ---: | ---: |
| 検証曲A | 従来 | 39 | 0 |
| 検証曲A | BTC | 91 | 40 |
| 検証曲A | BTC＋弱い平滑化 | 92 | 41 |
| 検証曲A | ChordNet | 243 | 217 |
| 検証曲B | 従来 | 41 | 2 |
| 検証曲B | BTC | 36 | 6 |
| 検証曲B | BTC＋弱い平滑化 | 36 | 6 |
| 検証曲B | ChordNet | 105 | 79 |

約1分の音源に対する各モデルの別プロセス起動～出力はCPUで約7～8秒（初回ウォームアップを除く）。時間付き正解コードがないため、ルート/三和音/7th/boundaryの正解率は未評価。区間数の減少を精度向上と解釈しない。特に検証曲AのBTC、両曲のChordNetでは短い切り替わりが多い。弱い平滑化の改善も確認できない。既定値の変更は行わず、保存した各方式を試聴比較する用途に留める。

自動テストで空出力・NaN・重複・空白・長い末尾欠落を拒否、元ラベル保持とconfidence非付与、strict=False再試行拒否、統計欠落拒否を確認。UIテストでは推定方式をIPCへ渡すこと、方式別比較で手修正を変更しないこと、独立したモデルセットアップを確認。ブラウザーハーネスで実曲の4方式選択とBTC表示への切り替えを確認。ネイティブGUIの自動操作は行っていない。

`npm run check`成功（UI30件、Python29件、Rust2件、TypeScript/Vite、rustfmt/clippy）。`npm run tauri:build -- --no-bundle`成功。更新したWindows実行ファイルはsrc-tauri/target/release/music-sweeper.exe。

## BTCへの統一（2026-09-21）

ユーザー指定によりコード推定はBTC・原曲・追加平滑化なしへ固定。方式選択と方式別比較UI、従来三和音/Viterbi、ChordNetと平滑化の実行分岐を削除。キーは従来のクロマ・調性プロファイルを独立関数として保持。新runでは比較データを生成せず、過去のrunと手修正を保持する。旧方式の保存結果には再推定の案内を表示する。

初回セットアップにBTCを含め、既存環境で未準備の場合はコード・全体分析を無効にして専用セットアップを案内する。声の分類だけの解析は引き続き実行可能。BTCが失敗しても別方式へ置き換えない。

「検証曲A」「検証曲B」の参照プロジェクトで方式指定なしのコード再解析を実行し、従来のBTC出力と完全一致（91区間/36区間）。キー・歌詞・拍・声の分類・手修正が不変、旧runファイルのバイト列が不変であることをassertで確認。npm run check成功（UI30、Python29、Rust2）。

## ヘッドレス・MCP・GUI連携（2026-09-21）

`npm run check`成功: UI35件、Python42件、公式MCP SDKを使ったプロセス間統合1件、Rust2件（計80件）。TypeScript/Vite、rustfmt、clippyも成功。`npm run tauri:build -- --no-bundle`でWindows実行ファイルを更新。

検証対象は許可範囲、JSONエラー、永続ジョブ、プロセス間の保存ロック、リビジョン競合、編集の再送・途中失敗からの再試行、音声クリップ、MCPリソース、外部変更とGUIの未保存修正・未適用入力の保護。PowerShellのセットアップとPython解析のロック競合も実プロセスで確認した。

`scripts/verify-automation.mjs`を使い、許可済み2曲の参照プロジェクトでMCPからBTCのharmonyジョブを起動。両方completeまで追跡でき、検証曲A91区間、検証曲B36区間を取得。元音源コピー・再生用音声のSHA-256、手修正、コードとキー以外のトラックが保持されることをassertで確認。歌詞・分離・声分類のモデルを全て再実行する試験ではない。

更新した実際のWindowsアプリを起動し、ヘッドレスAPIから保存済み「検証曲A」の12〜18秒・声の表現トラックを指定。約2秒でGUIからapplied応答を確認した。音源・解析結果は変更していない。自動再生はしない。表示操作のネイティブIPC経路の確認であり、目視によるレイアウト評価ではない。

ローカル検証ログ: test-results/gui-stage4-check.log、test-results/mcp-real-songs.log、test-results/automation-native-build.log、test-results/native-display-verification.json。音源や実曲結果・ローカルMCP設定はGitに含めない。

## AST / YAMNetの比較（2026-09-21）

公式google/yamnet/1 SavedModelを取得。アーカイブSHA-256はb80da2a1a56926fb0767205051a200dd7b3beaf3ea1ea126c42a53943996e5e0。独立Python3.11、TensorFlow2.20.0、CPUで推論。0.2秒・10.095秒・11.2秒・19.695秒の入力で、分割推論と公式モデルの一括推論の各対象クラスのスコアが一致（rtol=1e-4、atol=1e-6）。公式モデルの端点パディングで追加される窓も最終チャンクで保持する。

MCPから許可済み2曲の参照プロジェクトへscope=vocal-comparisonを実行し、どちらもcomplete。原曲コピーと再生用音声のSHA-256、手修正、全通常トラック、波形等のseries、分離音声参照の不変をassertした。比較取得の概要とページングも実MCP経由で確認した。

| 曲 | 検出器・入力 | ビートボックス候補 | ブレス候補 | ハミング候補 |
| --- | --- | ---: | ---: | ---: |
| 検証曲A | AST・原曲 | 2 | 0 | 0 |
| 検証曲A | AST・分離ボーカル | 1 | 4 | 2 |
| 検証曲A | YAMNet・原曲 | 2 | 0 | 0 |
| 検証曲A | YAMNet・分離ボーカル | 0 | 3 | 2 |
| 検証曲B | AST・原曲 | 1 | 4 | 0 |
| 検証曲B | AST・分離ボーカル | 0 | 6 | 1 |
| 検証曲B | YAMNet・原曲 | 1 | 2 | 0 |
| 検証曲B | YAMNet・分離ボーカル | 2 | 3 | 8 |

ASTは開始/継続0.15、YAMNetは開始0.15/継続0.10の仮基準。通常の打撃音補助検出は表に含めない。異なる解析窓・閾値の候補数であり、認識精度の順位ではない。時刻付きの正解注釈はないため適合率・再現率・境界誤差は未評価。特に「検証曲A」のYAMNet分離ボーカルはビートボックス候補0件で、既存ASTを置き換える根拠にはならない。

ブラウザーハーネスで実曲の4系列、分類切り替えを確認。YAMNetボーカルのハミング候補を押し、音声がvocals.wav、位置8.16秒、ループON、再生停止のままであることをDOMと実音声要素で確認した。これはブラウザーハーネスのUI検証で、ネイティブ画面の操作検証ではない。

`npm run check`成功（UI38、Python47、MCP統合1、Rust2、合計88件）。TypeScript/Vite、rustfmt/clippy成功。`npm run tauri:build -- --no-bundle`成功。ログはtest-results/yamnet-stage2-check.log、yamnet-framing-check.log、yamnet-real-songs.log、yamnet-native-build.log。モデル・実曲・比較JSONはGitに含めない。
