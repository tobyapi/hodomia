# 公開前の情報確認

確認日: 2026-09-21。対象コミット: `bcc7f95`（README追記前）。
この記録には見つかった個人メールアドレスやユーザーディレクトリの全文を転記していない。

## 調査範囲

Git追跡中の231ファイル、全ローカル参照から到達できる25コミット・435個のファイル内容を検査した。同じ内容のGitオブジェクトは一度だけ検査した。コミットの作者・コミッターとメッセージも確認した。

秘密鍵の開始行、代表的なGitHub/OpenAI/Anthropic/Hugging Face/AWS/Slackトークン、認証値の代入、認証情報付きURL、メールアドレス、ホームディレクトリのパスをパターン検索した。認証関連語とローカルパスはソースも確認した。専用の秘密情報スキャナーは未導入で、この検索による未検出は秘密情報が存在しない保証ではない。

到達不能なGitオブジェクト、reflog、外部サービス上のPR・Issue・CIログ・リリース添付は対象外。Git除外ディレクトリの内容は全件走査していない。リポジトリの公開設定や履歴は変更していない。

## 公開前に対応を決める情報

| 優先度 | 場所 | 確認した内容 | 対応案 |
| --- | --- | --- | --- |
| 高 | Git履歴のauthor / committer | 個人のGmailアドレスが全25コミットに入っている。表示名も公開される | 公開用メールアドレスを決める。過去分も伏せるなら履歴書き換え、または履歴を含めない公開用リポジトリを検討する。今後のGit設定変更だけでは過去分は変わらない |
| 中 | `docs/automation.md:54` | MCP設定例に実際のWindowsユーザー名を含むDownloadsパスがある | `$env:USERPROFILE`や架空の例へ置換する。過去の版にも存在する |
| 中 | `docs/verification.md:25`以降 | 実曲名、解析値、候補時刻、run ID、利用者の判断・修正内容が残っている | 公開してよい検証記録か判断し、必要なら曲A/Bへ匿名化して固有IDや利用者とのやり取りを整理する。現在のファイルから消しても過去の版に残る |
| 低 | `src/api.test.ts:8–9` | 日本語パス試験に実曲と同じ名前が使われている | 日本語・長いWindowsパスのテスト条件を保ち、曲名を架空の文字列へ置換できる。ユーザー名部分はテスト用の一般名で、実ユーザー名の検出ではない |
| 低 | `AGENTS.md:2`、`.clean/decisions.md:13`、`docs/quality-gate.md:3` | 別のローカルプロジェクトの名称・配置が分かる | 公開ドキュメントに必要か確認し、必要がなければ一般化する |
| 低 | `README.md:5–12`、モデル準備の説明 | 開発PCの配置・準備済み状態を前提としている | 新規利用者向けの起動・初回セットアップ手順へ整理する |

実曲名の検索では現在の`docs/verification.md`で43行、`src/api.test.ts`で2行が一致した。既知の曲名以外の固有名詞を網羅的に識別した件数ではない。上流スキルの著作権表示・作者名・取得元は第三者の来歴であり、個人情報の一括置換で削除しない。

## Gitに含まれていなかったもの

検査したファイル内容では、上記パターンに合うAPIトークン・秘密鍵・認証値の代入・認証情報付きURLは見つからなかった。ファイル本文にメールアドレスはなく、個人メールはコミットメタデータにあった。

到達可能な履歴のファイル名には、音源（MP3/MP4/M4A/WAV/FLAC）、重み（PTH/ONNX/safetensors）、`.env`、PEM/KEY/P12、ZIP/7zに該当するものはなかった。NULを含むバイナリとして検出したのはアプリアイコン2点だけで、1MB超のGit blobもなかった。

`.runtime/`、`test-results/`、`src-tauri/target/`などは`.gitignore`で除外されている。ただし作業フォルダーを丸ごとZIPにすると、実曲や解析結果、モデル、ビルド生成物まで含む可能性がある。公開用ファイルはGit追跡対象を基準に作る。

現在の`.gitignore`には`.env`や任意の場所の音源を一律に除外するルールはない。今回の未混入を将来も保つための追加候補になる。秘密情報検出のフック・CIも別途検討できる。

## OSS化に関する未確認事項

リポジトリ本体の`LICENSE`はなく、`package.json`と`src-tauri/Cargo.toml`にもライセンス指定がない。同梱スキル2件のLICENSEは保持されているが、本体のライセンス選択は別途必要。今回はこちらで選定していない。

READMEにあるとおり、BTCモデル重みの商用利用・再配布条件は未確認。依存ライブラリ・各モデル・配布バイナリの条件を網羅した調査も今回の範囲外。ソースの公開と、モデルや依存物を含むインストーラーの配布は分けて確認する。

## JSONを共有する場合

`analysis/project_export.py`はスナップショット全体を書き出す。`root`の絶対パス、曲名、原本ファイル名、歌詞・手入力、失敗メッセージ、モデル来歴を含み、匿名化処理はない。OSSのサンプルにはREADMEの架空データを使い、実曲の`analysis.json`をそのまま追加しない。

## 次の作業順

1. 公開する作者名・メール、実曲の検証記録の扱い、本体ライセンスを決める。
2. パス例と検証記録を整理し、JSONの公開サンプルは架空データにする。
3. 必要ならバックアップを取って履歴を整理し、公開対象の全履歴を再検査する。
4. 追跡ファイルと配布物を別々に確認して公開する。

今回の変更はREADMEのJSON説明とこの調査記録のみ。検出箇所の削除、履歴の書き換え、GitHubへの公開は行っていない。

READMEの架空JSONは構文と行データを検証し、実際の書き出し処理の出力と一致することを確認した（テスト用の`root`のみ差し替え）。`npm run check`は全95テスト、品質・依存検査、型検査、Viteビルド、rustfmt、Clippyが成功した。
